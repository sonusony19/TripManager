<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.trip.manager">

    <application
        android:name=".application.TMApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TripManager">
        <activity
            android:name=".ui.trip.view.TripDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.user.view.CreateUserActivity"
            android:exported="true"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity
            android:name=".ui.login.view.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".ui.home.view.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.splash.view.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>